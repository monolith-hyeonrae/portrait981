"""CLI entry points for visualbind (future).

Planned commands:
- ``visualbind analyze`` — Day 0 analysis (N_eff, correlation, distributions)
- ``visualbind train`` — fit strategies from catalog data
- ``visualbind eval`` — compare strategies on test set
"""

from __future__ import annotations


def main() -> None:
    """CLI entry point (placeholder)."""
    print("visualbind CLI — not yet implemented. See visualbind.analyzer for programmatic API.")
